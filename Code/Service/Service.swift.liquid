//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import RxSwift

protocol {{ prefix }}{{ module_info.name }}ServiceProtocol {
    func getList() -> Observable<[Any]>
}

final class {{ prefix }}{{ module_info.name }}Service {
    private let apiClient: ApiClient
    private let mapper: MapperProtocol
    init(apiClient: ApiClient = ApiClient(), mapper: MapperProtocol = Mapper()) {
        self.apiClient = apiClient
        self.mapper = mapper
    }
}

extension {{ prefix }}{{ module_info.name }}Service: {{ prefix }}{{ module_info.name }}ServiceProtocol {
    
    func getList() -> Observable<[Any]> {
        let request = {{ prefix }}{{ module_info.name }}NetworkRouter.getList
        return apiClient.load(request: request)
            .flatMap(mapper.map)
    }
}
