
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import RxSwift

protocol {{ prefix }}{{ module_info.name }}Service {

    func getList() -> Observable<()>
}

final class {{ prefix }}{{ module_info.name }}ServiceImp {
    private let apiClient: ApiClient
    private let mapper: MapperProtocol
    init(apiClient: ApiClient = ApiClient(), mapper: MapperProtocol = Mapper()) {
        self.apiClient = apiClient
        self.mapper = mapper
    }
}

extension {{ prefix }}{{ module_info.name }}ServiceImp: {{ prefix }}{{ module_info.name }}Service {
    
    func getList() -> Observable<()> {
        let request = {{ prefix }}{{ module_info.name }}NetworkRouter.getList
        return apiClient.load(request: request)
            .flatMap(mapper.void)
    }
}
