
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import Alamofire

enum {{ prefix }}{{ module_info.name }}NetworkRouter: URLRequestConvertible {

    case getList
}

extension {{ prefix }}{{ module_info.name }}NetworkRouter: NetworkRequestParams {
    
    var path: String {
        switch self {
        case . getList:
            return "/list/"
        }
    }
    
    var method: HTTPMethod {
        switch self {
        default:
            return .get
        }
    }
    
    var parameters: Parameters {
        switch self {
        default:
            return [:]
        }
    }
    
    var encoding: ParameterEncoding {
        switch self {
        default:
            return URLEncoding.default
        }
    }
}
